# ROC-SYSTEM 部署密钥配置（示例）
# 复制为 scripts/config/secrets.env（不要提交到 git）

# 数据库密码（必填；生成强随机值后填写，留空时部署脚本拒绝启动）
DB_PASSWORD=

# Device token 不属于部署级共享密钥；由平台逐车签发，不写入本文件。

# 如需：registry 登录（未来如果采用镜像仓库分发）
# REGISTRY_USER=
# REGISTRY_PASSWORD=

